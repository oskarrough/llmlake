{
  "name": "llmlake",
  "private": true,
  "scripts": {
    "check": "vp check --fix",
    "collect-all": "bun run collect-all.ts",
    "build-all": "bun run build-all.ts",
    "query": "./query"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "vite-plus": "^0.1.20"
  }
}
