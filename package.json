{
  "name": "llmlake",
  "private": true,
  "scripts": {
    "check": "vp check --fix",
    "collect": "bun run collect.ts",
    "build": "bun run build.ts",
    "query": "./query"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "vite-plus": "^0.1.20"
  }
}
