---
id: freeform-b
heading: (chosen by the agent)
when: include only if you have a genuinely separate observation from freeform-a
---

Second open slot — same rules as `freeform-a`. **Omit entirely** if your
second-best observation isn't strong enough to stand on its own. A short
report with one freeform section beats a padded report with two.

## Rules

- Same as `freeform-a`. Cite a query. Specific heading. No padding.
